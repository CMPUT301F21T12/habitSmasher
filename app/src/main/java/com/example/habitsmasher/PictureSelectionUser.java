package com.example.habitsmasher;
import android.net.Uri;

/**
 * Interface for all Fragments that allow users to select their own image
 */
public interface PictureSelectionUser {
    void setImage(Uri image);
}
